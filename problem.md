# 已知问题记录：辅助头（aux）学习偏弱 / 均值回归现象

## 现象

- 推理侧常见 `attack≈0.5`、`danger` 中等、`tactical` 长期贴在低位（如 0.03～0.08），跨局面变化小。
- `val_aux_mse` 可以很低，但**不一定**表示学到了「判别局面类型」——在标签高度偏斜时，**常数或近常数预测**即可压低 MSE。

## 根因分析（与当前实现一致）

实现见 `nn/src/nn/aux_pseudo_labels.py` 与 `crates/xiangqi_dataset/src/aux_labels.rs`（Python/Rust 对齐）：

| 头 | 当前语义（摘要） | 与「尖锐监督」的差距 |
|----|------------------|----------------------|
| **attack** | 行棋方物质差经 `tanh(adv/12)` 映射到 [0,1]，均势附近集中在 0.5 一带 | 信号平滑、方差小，大量步数难以区分 |
| **danger** | 合法着数量（机动）+ 物质压力的凸组合，再截断到 [0,1] | 非「将死距离 / 王暴露」等强语义 |
| **tactical** | 合法着中**吃子着**所占比例 | 多数局面比例低且分布挤在低端；少数对杀才高，**类别极不平衡** |

因此模型易出现 **均值回归**：在当前标签与损失下，拟合全局边际分布往往是较优的省力解。

## 数据分布（完整对局）

- 数据集以**完整对局**为主时，步数分布偏向开局、均势周旋、子力仍全在棋盘等「典型」局面。
- 这会放大：**attack / tactical 真值直方图挤在中间或低端**，尖锐局面成为少数类；若不做加权/过采样，网络不会为少数类单独塑形。

## 训练侧因素

- 辅助头为 sigmoid + MSE，且 `aux_loss_weight` 相对 policy CE 通常较小时，**梯度竞争**不利于学细粒度 aux。
- **value** 若用 `2*attack-1` 作 bootstrap 监督，会继承 attack 的平滑性（与主线 value 路线相关时需心里有数）。

## 验证建议（优于单看 MSE）

- 按局面类型抽样（安静 / 对杀 / 大亏或大胜 / 多吃子选择等），看三头输出**是否跨类拉开**。
- 画 **真值与预测** 的直方图、按真值分箱的 MAE/相关，识别是否「整条预测带贴一条窄带」。
- 对 **tactical** 单独看分位数与少数类上的表现。

## 后续可能方向（未承诺排期）

- **标签**：更深或更尖的监督（例如与浅深搜索差、MultiPV 分裂等相关的量）需要额外算力与数据管线，与当前「零搜索伪标」是不同取舍。
- **数据**：对高 `tactical`、极端 `attack`/`danger` 样本过采样或损失加权。
- **损失**：针对极不平衡头的加权 MSE、Focal、或分箱再校准等（需实验）。

---

*记录日期：便于与主线里程碑对照；修复或改标签定义后应更新本节或归档。*
