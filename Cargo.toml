[workspace]
resolver = "2"
members = [
    "crates/xiangqi_core",
    "crates/engin",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false