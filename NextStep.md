# NextStep

**P1 已结束**。P2 **暂缓**（用户确认先不做并排 diff / 细锁等）。

**当前建议主线**：px0 数据上的 policy 训练与 value（固定 `q_ratio` 的 qMix）闭环；搜索侧仅做小修，不再扩 P2 对齐项。

---

## 历史：P1 硬约束（已完成）

- **先完整对齐 `px0 classic search`**
- **先抄清楚，再实现**
- **先把 `px0` 做出来，再讨论吸收 `KataGo`**
