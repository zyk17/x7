# NextStep

当前地基已经收敛到这条主线：

1. `xiangqi_core`：规则核心
2. `engin`：MCTS / ONNX / UCI
3. `xiangqi_dataset`：PGN / XRSH v5 / 搜索标注
4. `nn`：policy + value(search_q) 训练与导出

下一阶段目标只保留三个：

1. 做出稳定可用的 `policy + value` baseline
2. 做出小批量、分轮次、受控混合的数据闭环
3. 在不明显破坏人类特征的前提下，让搜索和 value 逐轮变好

实现顺序建议：

1. `nn`
   先把纯人类数据上的 `policy` baseline 跑通，再补 `value(search_q)` baseline。
2. `xiangqi_dataset`
   把大师/人类数据补成更适合搜索标注的样本集，特别是冷门局面、偏离主流开局但仍有人类合理性的局面。
3. `engin`
   在已有 MCTS 地基上继续补标准 `info` 输出、搜索统计、根节点分布观测，服务训练标注和线上对弈。
4. 小批量闭环
   用当前网络跑少量受控搜索/自对弈，生成一轮搜索标注数据，与人类数据按固定策略混合训练；一轮结束先评估，再决定是否继续下一轮。

当前阶段的明确边界：

1. 不回头加 Alpha-Beta 主线
2. 不回头加辅助头、多任务头
3. 不再新增正式数据格式，只保留 `XRSH v5`
4. 不为未来社区训练平台提前做大抽象

我建议你下一步就按这个节奏推进：

1. 先做训练 baseline
2. 再做受控搜索标注数据补充
3. 再做第一轮混合训练实验

你实现后，我负责按这个阶段目标做 review。
