# 工程目标

本文件将历次路线讨论（曾见于沟通草稿）整理为本仓库的**中期工程目标**、**交付阶段**与**可执行任务**。已定稿的设计摘要见 **`docs/design-summary.md`**。

相关文档：

- 架构与边界：`ARCHITECTURE.md`
- 里程碑定义：`MILESTONES.md`
- 执行清单：`TODO.md`

---

## 1. 总目标

本项目不走以下两条单一路线：

- 不是传统的 `eval + alpha-beta` 固定启发式路线
- 也不是纯 `policy` 直接替代搜索的路线

目标路线是：

**认知驱动搜索（Cognition-Driven Search）**

职责分工：

- 模型负责：局面理解、候选着聚焦、风险识别、战术感知、搜索资源建议
- 搜索负责：正确性验证、强制变化补全、极限战术计算、终局收束

## 1.1 双主线

本项目存在两条**并行且互相增益**的主线：

- **短期主产品：复盘系统**
  - 目标是让模型学会**可解释的人类风格与局面语义**
  - 直接服务复盘分析、候选着解释、危险提示、战术提醒
- **长期路线：搜索引擎**
  - 目标是让搜索验证模型是否真的学会了有用的棋感
  - 再逐步把这些语义信号转化成搜索收益

因此本项目不是“模型或搜索”二选一，而是：

- 模型先服务复盘系统
- 搜索再验证并放大模型中真正有用的棋感

这条路线在中国象棋上的价值尤其高，因为象棋具备：

- 高战术密度
- 强制变化频繁
- 搜索爆炸明显
- 人类棋感对分支筛选影响大

---

## 2. 当前判断

截至当前仓库状态，主问题已经从“提高 policy 指标”转为：

**让系统知道哪些局面该搜、该搜多深、该搜多宽。**

但这不是唯一方向。当前实际目标分成两层：

- **短期**：让模型输出足够稳定、可解释，能进入复盘系统
- **长期**：让这些语义信号在搜索中产生真实收益

因此，P3 之后的核心方向是：

- value
- search-aware heads
- search visit distillation
- dynamic search

---

## 3. 分阶段目标

- **G0 / P0–P3**：完成规则、数据、训练、UCI 搜索闭环
- **G1 / P4**：补齐 value，服务搜索稳定性与资源分配
- **G2 / P5**：让现有语义头直接进入搜索消费链路
- **G3 / P6**：学习搜索注意力，而不只是学习最佳着
- **G4 / P7**：实现动态搜索调度

说明：

- 详细阶段定义见 `MILESTONES.md`
- 近期实际执行见 `NEXT_STEPS.md`

---

## 4. 成功标准

不是单看 policy 指标，而是看以下四类结果：

### S1. 训练侧

- policy 收敛稳定
- value / aux 头可解释且可复现
- 标签生成完全统一到 Rust 规则源

### S1.5. 复盘产品侧

- head 输出能被复盘系统消费
- 标签语义对用户可解释，而不是只对 loss 有意义
- 候选着、危险、战术提示与人类理解一致

### S2. 搜索侧

- 相同时间预算下，节点利用率优于纯手工启发基线
- 危险局面与强制变化的命中率更高
- 剪枝与扩展决策能被 head 信号稳定改善

### S3. 引擎侧

- UCI 可稳定运行
- ONNX 推理与搜索联调稳定
- 有可复现的基准局面、固定时间与固定深度测试

### S4. 研发侧

- 每个 head 都有清晰语义、来源、用途、消费方
- 数据、训练、导出、引擎四层契约清晰
- 文档能直接指导下一阶段开发，而非只描述理念

---

## 5. 当前推荐执行顺序

1. 先做实 `P3`
2. 同时把现有 head 往“可解释、可复盘消费”方向收紧
3. 再做 `P4 value`
4. 然后只保留真正能改善复盘质量或搜索收益的 head
5. 最后才进入蒸馏与动态搜索

具体周计划与实验顺序见 `NEXT_STEPS.md`。

---

## 6. 当前禁止事项

- 不要为了追求“更大模型”而跳过搜索控制信号设计
- 不要在训练步重新引入多规则源在线标签逻辑
- 不要在 `engin` 中混入数据生成、标注或训练逻辑
- 不要把 value head 做成孤立评分头而不规划其搜索用途
- 暂时不要做 `style / sacrifice / initiative / psychological`

---

## 7. 与沟通草稿、归档摘要的关系

根目录 **`temp.md`** 仅为与本助手 **实时沟通** 的可清空草稿区；**非**权威来源。路线摘要归档至 **`docs/design-summary.md`**。

后续一律以本文件作为正式工程目标文档，并由：

- `MILESTONES.md` 维护阶段定义
- `TODO.md` 维护可执行任务

进行落地。
