# 工程目标

本文件将历次路线讨论（曾见于沟通草稿）整理为本仓库的**中期工程目标**、**交付阶段**与**可执行任务**。已定稿的设计摘要见 **`docs/design-summary.md`**。

相关文档：

- 架构与边界：`ARCHITECTURE.md`
- 里程碑定义：`MILESTONES.md`
- 执行清单：`TODO.md`

---

## 1. 总目标

本项目不走以下两条单一路线：

- 不是传统的 `eval + alpha-beta` 固定启发式路线
- 也不是纯 `policy` 直接替代搜索的路线

目标路线是：

**认知驱动搜索（Cognition-Driven Search）**

职责分工：

- 模型负责：局面理解、候选着聚焦、风险识别、战术感知、搜索资源建议
- 搜索负责：正确性验证、强制变化补全、极限战术计算、终局收束

这条路线在中国象棋上的价值尤其高，因为象棋具备：

- 高战术密度
- 强制变化频繁
- 搜索爆炸明显
- 人类棋感对分支筛选影响大

---

## 2. 当前判断

截至当前仓库状态，项目已经基本证明：

- 小型 policy 网络可以学到人类棋感
- 模型具备较强的候选着聚焦能力
- 多头辅助标签适合继续向“搜索控制信号”演进

当前最大短板已不再是“单纯提高 policy top1/topk”，而是：

**让系统知道哪些局面该搜、该搜多深、该搜多宽。**

因此，P3 之后的核心不应只是“做一个能下 UCI 的引擎”，而应逐步进入：

- value 估计
- search-aware heads
- search visit distillation
- dynamic search

---

## 3. 分阶段工程目标

### G0. 完成可用基础闭环

目标：

- `xiangqi_core` 稳定提供合法着与规则语义
- `xiangqi_dataset` 稳定产出 XRSH
- `nn/` 稳定完成训练与 ONNX 导出
- `engin` 完成最小 UCI + Alpha-Beta 骨架

对应关系：

- 等价于当前 `P0` ~ `P3`

### G1. 补齐 value 能力

目标：

- 在 shared trunk 上加入 value head
- 让网络不仅知道“该走什么”，还知道“局面大致真值如何”

value head 的直接用途：

- 改善搜索稳定性
- 降低 horizon effect
- 为深度/宽度分配提供基础信号
- 为后续局面复杂度与可塑性判断提供底座

### G2. 让辅助头直接服务搜索

目标：

- 将当前辅助头从“训练语义增强”推进到“搜索控制接口”

优先考虑的头：

- `danger`: 当前局面是否需要扩深
- `volatility`: 当前局面是否存在高波动/高战术密度
- `forcing`: 是否存在强制变化
- `mobility_tension`: 局面是否紧绷、是否适合保守剪枝

要求：

- 标签定义须可由 `xiangqi_core` / `xiangqi_dataset` 在 Rust 侧统一生成
- 训练与引擎消费同一语义，不再依赖多规则源在线推断

### G3. 学习“搜索注意力”而不是只学最佳着

目标：

- 引入 search visit distillation
- 学习搜索把时间投入到哪些着法、哪些节点，而不只学习最终 best move

训练目标不只是：

- `best move`

更关键的是：

- `visit count`
- `search distribution`
- `resource allocation preference`

### G4. 动态搜索

目标：

- 让模型输出的控制信号真正参与搜索调度

搜索动态控制项：

- extension
- late move reduction
- late move pruning
- null move 使用时机
- beam / top-k 宽度
- aspiration window 调整

最终目标：

- 平稳局面少算
- 危险局面算透
- 多分支复杂局面适当扩宽

---

## 4. 成功标准

不是单看 policy 指标，而是看以下四类结果：

### S1. 训练侧

- policy 收敛稳定
- value / aux 头可解释且可复现
- 标签生成完全统一到 Rust 规则源

### S2. 搜索侧

- 相同时间预算下，节点利用率优于纯手工启发基线
- 危险局面与强制变化的命中率更高
- 剪枝与扩展决策能被 head 信号稳定改善

### S3. 引擎侧

- UCI 可稳定运行
- ONNX 推理与搜索联调稳定
- 有可复现的基准局面、固定时间与固定深度测试

### S4. 研发侧

- 每个 head 都有清晰语义、来源、用途、消费方
- 数据、训练、导出、引擎四层契约清晰
- 文档能直接指导下一阶段开发，而非只描述理念

---

## 5. 当前推荐执行顺序

1. 完成 `P3`：做出最小可联调搜索引擎闭环
2. 在 `nn/` 中加入 `value head`
3. 在 `xiangqi_dataset` 中设计并落地 Rust 侧 value / search-aware 标签生成
4. 在 `engin` 中将 value / aux 头接入搜索控制
5. 再推进 search visit distillation 与 dynamic search

---

## 6. 当前禁止事项

- 不要为了追求“更大模型”而跳过搜索控制信号设计
- 不要在训练步重新引入多规则源在线标签逻辑
- 不要在 `engin` 中混入数据生成、标注或训练逻辑
- 不要把 value head 做成孤立评分头而不规划其搜索用途

---

## 7. 与沟通草稿、归档摘要的关系

根目录 **`temp.md`** 仅为与本助手 **实时沟通** 的可清空草稿区；**非**权威来源。路线摘要归档至 **`docs/design-summary.md`**。

后续一律以本文件作为正式工程目标文档，并由：

- `MILESTONES.md` 维护阶段定义
- `TODO.md` 维护可执行任务

进行落地。
