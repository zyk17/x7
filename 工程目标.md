# 工程目标

本文件整理本仓库的**中期工程目标**、**交付阶段**与**推荐顺序**。

相关文档：

- 架构与边界：`ARCHITECTURE.md`
- 里程碑定义：`MILESTONES.md`
- 近期执行：`NEXT_STEPS.md`
- 阶段入口：`TODO.md`

---

## 1. 总目标

本项目不走以下两条单一路线：

- 不是传统的“固定 eval + alpha-beta”路线
- 也不是“纯 policy 直接替代搜索”路线

目标路线仍然是：

**认知驱动搜索（Cognition-Driven Search）**

职责分工：

- 模型负责：候选着、趋势、风险、攻势等人类可理解语义
- 搜索负责：战术穷尽、强制变化验证、终局正确性

---

## 1.1 双主线

本项目存在两条并行主线：

- **短期主产品：复盘系统**
  - 最小目标不是 `policy only`
  - 当前 MVP 目标是：`policy + value + danger + attack`
  - 复盘系统允许结合**真理引擎**与**滑动窗口前后文**
- **长期路线：搜索引擎**
  - 搜索负责验证这些语义是否真的有用
  - 再逐步把它们转成搜索收益

因此本项目不是“模型或搜索”二选一，而是：

- 模型先服务复盘系统
- 搜索再验证并放大模型中真正有用的棋感

---

## 2. 当前判断

截至当前状态，核心问题已经不是“policy 能不能再提一点”，而是：

**如何尽快做出最小可解释复盘系统。**

当前判断如下：

- `policy / trunk` 已证明还在持续学习
- `value` 已证明可以从强 trunk 中单独读出
- `danger / attack` 更适合在强 trunk 上单独训练
- `tactical` 暂时不是复盘 MVP 阻塞项

所以近期主线不是“继续同时共训所有头”，而是：

1. 继续做强 `policy + trunk`
2. trunk 接近平台后，冻结 trunk 单独训练 `value`
3. 再冻结 trunk 单独训练 `danger`
4. 再冻结 trunk 单独训练 `attack`
5. 将这些输出与真理引擎、滑动窗口整合成完整复盘解释

---

## 3. 分阶段目标

- **G0 / P0–P3**：完成规则、数据、训练、UCI 搜索闭环
- **G1 / P4**：在强 trunk 上补齐复盘 MVP 头：`value / danger / attack`
- **G2 / P5**：把模型头、真理引擎、滑动窗口整合成稳定复盘系统
- **G3 / P6**：将复盘已验证有效的头逐步接入搜索
- **G4 / P7**：蒸馏与动态搜索

---

## 4. 成功标准

### S1. 训练侧

- `policy / trunk` 收敛稳定
- `value / danger / attack` 能在强 trunk 上稳定读出
- 数据、训练、导出契约一致

### S2. 复盘产品侧

- `policy` 能提供候选着与排序
- `value` 能提供趋势判断
- `danger` 能提供风险提示
- `attack` 能提供攻势提示
- 真理引擎与滑动窗口能补足“客观对错”和“前后因果”

### S3. 搜索侧

- 后续接入时有可复现的 benchmark
- 只保留对搜索真正有收益的头

### S4. 研发侧

- 每个 head 都有清晰语义、来源、用途、消费方
- 文档能直接指导下一阶段开发

---

## 5. 当前推荐顺序

1. 先继续做强 `policy + trunk`
2. 再做 `value`
3. 再做 `danger`
4. 再做 `attack`
5. 再把它们整合进复盘系统
6. 最后才做搜索收益验证

---

## 6. 当前禁止事项

- 不要为了追求“更大模型”而跳过复盘解释设计
- 不要把 value 当成外部强引擎终评的唯一替代物
- 不要把单局面单时刻输出误当成完整复盘系统
- 不要在现有 head 未完成收益归因前继续扩新抽象标签
