# NEXT_STEPS

本文件只回答一个问题：

**接下来优先把什么做成产品，代码和训练各自先做什么。**

---

## 当前判断

当前仓库状态已经比较明确：

- `P0` / `P1` / `P2` 已打通
- `P3 engin` 已有基础能力，但不是短期主阻塞项
- `policy / trunk` 已证明还在持续学习
- `value` 已证明可以从强 trunk 中单独读出
- `danger / attack` 需要在强 trunk 上继续单头验证

因此近期目标不再是“同时共训所有 head”，而是：

1. 先做出最小复盘系统
2. 再考虑搜索收益验证

---

## 一、最小复盘系统

当前最小复盘系统目标是：

- `policy`
- `value`
- `danger`
- `attack`

它们分别承担：

- `policy`：候选着与排序
- `value`：局势趋势
- `danger`：风险提示
- `attack`：攻势提示

补充说明：

- `tactical` 暂时不是复盘 MVP 阻塞项
- 复盘系统允许结合**真理引擎**与**滑动窗口前后文**
- 模型不是唯一信息源，而是复盘解释的核心语义模块

---

## 二、当前总原则

- **先做强 `policy + trunk`**
- **再冻结 trunk 分别训练 `value / danger / attack`**
- **先做复盘解释闭环，再做搜索消费闭环**
- **不新增 head 种类**
- **不要求近期把所有头立即接进搜索**

一句话：

**先做最小可解释复盘系统，再谈搜索收益。**

---

## 三、写代码时优先做什么

### Priority A：复盘消费链路

#### A1. 固定复盘输出契约

- [ ] 固定推荐着 + Top-k 候选着格式
- [ ] 固定 `value / danger / attack` 的数值到文案映射
- [ ] 固定模型输出、真理引擎结果、滑动窗口上下文的组合格式

#### A2. 重写复盘编排层

- [ ] 保留“模型不是唯一信息源”的原则
- [ ] 真理引擎负责战术校验、客观优劣、关键变化
- [ ] 滑动窗口负责解释前因后果，而不是只看单点局面
- [ ] 将当前试验性实现重写成稳定版本

### Priority B：单头训练工具

- [ ] 保持 `freeze trunk`、`freeze policy head`、`freeze value head` 能力稳定可用
- [ ] 给 `value / danger / attack` 单头实验准备固定命令模板
- [ ] 固定实验目录结构，减少手工整理

### Priority C：搜索侧只做维护

- [ ] 保持 `engin`、benchmark、ablation 基础设施可用
- [ ] 暂不要求 `danger / attack` 立即进入搜索主线
- [ ] 引擎侧文档保留，但不作为近期主阻塞项

---

## 四、不写代码时训练什么

### Priority T1：先继续做大一轮 trunk 训练

- [ ] 只以 `policy + trunk` 为主目标
- [ ] 直到 trunk 接近平台

### Priority T2：冻结 trunk 后按顺序做单头

1. [ ] `value`
2. [ ] `danger`
3. [ ] `attack`
4. [ ] `tactical`（第二波）

### Priority T3：每个头的判据

- [ ] 训练侧是否稳定优于简单基线
- [ ] 是否能生成可解释复盘提示
- [ ] 是否值得进入复盘 MVP

结论只允许三种：

- **进入 MVP**
- **保留待观察**
- **暂缓**

---

## 五、每周固定输出

- [ ] 一份 trunk 训练结果
- [ ] 一份单头实验结果或准备情况
- [ ] 一份复盘样例
- [ ] 一份结论：哪些能力已经够进入 MVP

每周复盘问题：

1. trunk 是否还在涨？
2. `value / danger / attack` 哪个最接近可用？
3. 复盘输出有没有更像人会说的话？
4. 真理引擎与滑动窗口是否帮解释更完整？
5. 哪个复杂度可以删掉？

---

## 六、建议的 4 周安排

### 第 1 周：trunk 大训练

- [ ] 跑一轮大的 `policy + trunk`
- [ ] 判断是否接近平台

### 第 2 周：`value` 单头

- [ ] 冻结 trunk，训练 `value`
- [ ] 固定复盘趋势文案

### 第 3 周：`danger` / `attack`

- [ ] 冻结 trunk，训练 `danger`
- [ ] 冻结 trunk，训练 `attack`
- [ ] 形成风险与攻势提示

### 第 4 周：复盘编排层

- [ ] 将模型头、真理引擎、滑动窗口整合
- [ ] 固定复盘输出格式
- [ ] 更新正式文档
