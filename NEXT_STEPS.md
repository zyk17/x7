# NEXT_STEPS

本文件只回答一个问题：

**接下来你写代码时做什么，不写代码时训练什么。**

假设当前状态为：

- `P0` / `P1` / `P2` 已基本打通
- `P3` 已有可运行基础，但仍需进一步做实
- 已存在多个 head（至少 `attack / danger / tactical / value` 路径），但当前效果不理想

项目当前是**双主线**：

- **短期主产品**：复盘系统，需要模型输出可解释的人类语义
- **长期路线**：搜索引擎，需要验证这些语义是否真的带来搜索收益

因此近期目标不是“再设计更多 head”，而是：

1. 让 `engin` 成为稳定实验平台
2. 让现有 head 的训练与解释质量有据可查
3. 让现有 head 的搜索收益有 benchmark 证明

---

## 一、近期总原则

- **不新增 head 种类**
- **先压缩变量，再做判断**
- **先做消费链路，再谈更复杂标签**
- **同时看复盘解释价值和搜索收益**
- **暂停凭感觉调参，改为 benchmark / ablation 驱动**

一句话：

**暂停扩张，先做归因。**

这里的“归因”分两条：

- 这个 head 是否改善复盘系统输出
- 这个 head 是否改善搜索行为

---

## 二、工作安排方式

你接下来可以按两类时间来安排任务：

### A. 写代码时

优先做**基础设施、消费链路、回归工具**。

这类任务的特点：

- 会影响后续所有实验
- 不依赖 GPU 空闲
- 做完后能让训练结果更容易被验证

### B. 不写代码时

优先做**训练、对比实验、结果归档**。

这类任务的特点：

- 可以长时间跑
- 需要固定配置与可复现输出
- 产出应该直接反馈给下一轮代码决策

---

## 三、写代码时优先做什么

### Priority A：把 `engin` 变成稳定实验平台

这是第一优先级。没有稳定搜索平台，head 的价值无法判断。

#### A1. 补齐 P3 最小验收

- [ ] 完成 `engin` 与 `xiangqi_core` 的走子、合法性、终局判定联调
- [ ] 明确 `go depth` / `go movetime` / `go infinite` 的稳定行为
- [ ] 确认 TT、静止搜索、迭代加深、根排序的行为不漂移
- [ ] 为 UCI 主循环建立最小回归用例

#### A2. 建立 benchmark harness

- [ ] 新建固定 FEN 基准集：开局、中局战术、将杀、复杂静面
- [ ] 固定输出：`bestmove`、PV、depth、seldepth、nodes、nps、time
- [ ] 支持固定 `depth` 与固定 `movetime` 两种模式
- [ ] 输出 JSON 或 CSV，便于横向比较不同模型和不同开关

#### A3. 建立 ablation 开关

- [ ] 支持 `policy on/off`
- [ ] 支持 `aux heads on/off`
- [ ] 支持 `value on/off`
- [ ] 支持根节点 policy 排序开关
- [ ] 支持叶子 value 评估开关

验收标准：

- 能一键比较“相同搜索器 + 不同 head 配置”的行为差异

### Priority B：先做最小消费链路

这里的消费链路分两条：

- **复盘系统消费**
- **搜索消费**

优先顺序是：先能解释，再看是否稳定改善搜索。

#### B1. `value` 的最小消费方案

- [ ] 在 `engin` 中接入叶子评估辅助
- [ ] 在 `engin` 中接入 aspiration / pruning 参考信号

限制：

- 暂时不要让 `value` 同时驱动太多搜索参数

#### B2. `danger` 的最小消费方案

- [ ] 只先接入 extension / reduction 判定

目标：

- 先观察它是否真的帮助“危险局面算深一些”

### Priority C：把实验结果沉淀成工具

- [ ] 为 benchmark 增加模型路径、开关配置、输出文件参数
- [ ] 固定实验目录结构，避免每轮手工整理结果
- [ ] 为常用实验写成脚本或命令模板
- [ ] 把“复盘样例 + 引擎结果”放到统一记录位置

---

## 四、不写代码时训练什么

### Priority T1：先做最小对比矩阵

至少比较以下 4 组：

- [ ] `policy only`
- [ ] `policy + attack/danger/tactical`
- [ ] `policy + value`
- [ ] `policy + attack/danger/tactical + value`

### Priority T2：每组都看三类结果

- [ ] 训练侧：loss、acc、aux/value mse
- [ ] 复盘侧：标签分布、均值塌缩、案例可解释性
- [ ] 引擎侧：固定时限 bestmove 命中率、PV 稳定性、nodes 利用率

### Priority T3：重点审查现有 4 个头

建议优先级：

1. `danger`
2. `tactical`
3. `value`
4. `attack`

原因：

- `danger` 最适合复盘提示“这里危险”，也最适合接 extension / reduction
- `tactical` 最适合复盘提示“这里是战术点”，也最可能与战术搜索形成联动
- `value` 很重要，但前提是它是独立、可解释的人类局面感
- `attack` 最容易沦为“训练有数值、产品和搜索都难消费”的头

### Priority T4：给每个 head 明确结论

对每个现有 head，都明确记录：

- [ ] 标签来源是什么
- [ ] 训练是否稳定
- [ ] 是否存在均值塌缩
- [ ] 是否能生成可解释复盘提示
- [ ] 搜索中谁消费它
- [ ] 加入后 benchmark 是否变好

结论只允许三种：

- **保留**：有明确复盘价值或搜索收益
- **冻结**：暂时无害，但用途不明
- **下线**：增加复杂度但无收益

---

## 五、每周节奏

### 本周主线

- 代码时间：优先推进 `P3 + benchmark + ablation`
- 训练时间：优先跑最小对比矩阵

### 每周固定输出

每周至少产出这 4 样东西：

- [ ] 一份代码侧进展：本周新增了哪些搜索工具或开关
- [ ] 一份训练侧结果：跑了哪些配置，指标如何
- [ ] 一份复盘样例：哪些 head 解释得通，哪些解释不通
- [ ] 一份结论：保留、冻结、下线哪些复杂度

### 每周复盘问题

1. 这周比较了哪些配置？
2. 哪个 head 在训练上更稳定？
3. 哪个 head 在复盘上更可解释？
4. 哪个 head 在引擎上真正有收益？
5. 哪个复杂度可以删掉？
6. 下周继续做减法，还是可以进入下一阶段？

---

## 六、建议的 4 周安排

### 第 1 周：搜索平台与基准

- [ ] 修完 P3 剩余联调项
- [ ] 建立固定 FEN benchmark 集
- [ ] 跑通统一输出格式
- [ ] 建立 `policy on/off` 开关

产出：

- 一个可重复跑的搜索基准工具
- 一组稳定测试局面

### 第 2 周：head 对比实验

- [ ] 跑 `policy only`
- [ ] 跑 `policy + aux`
- [ ] 跑 `policy + value`
- [ ] 跑 `policy + aux + value`
- [ ] 汇总训练指标、复盘解释样例与引擎指标

产出：

- 一份 head 收益对比表
- 一份保留 / 冻结 / 下线建议

### 第 3 周：最小消费接线

- [ ] 将 `value` 接入最小叶子评估辅助
- [ ] 将 `danger` 接入最小 extension / reduction
- [ ] 为这些接线增加开关

产出：

- “有 head 消费”和“无 head 消费”的可比结果

### 第 4 周：收敛与减法

- [ ] 下线无收益 head
- [ ] 固化有收益的 head 契约
- [ ] 更新 `ARCHITECTURE.md` / `TODO.md`
- [ ] 决定是否进入真正的 `P5`

产出：

- 一条更干净的主线
- 一个明确的 `P5` 入口

---

## 七、明确暂停事项

在完成上面的归因前，以下事项建议暂停：

- [ ] 新增更多辅助头
- [ ] 做大模型宽度/深度扩张
- [ ] 引入 search distillation
- [ ] 引入动态搜索全量联动
- [ ] 大规模重新造数
- [ ] 做 `style / sacrifice / initiative / psychological`

这些都属于**放大复杂度**，不属于**澄清问题**。

---

## 八、当前结论

当前不应该继续问：

- “还能不能再加一个 head？”

当前应该问：

- “现有 head 里，谁真的改善了复盘解释？”
- “现有 head 里，谁真的改善了搜索？”

因此，下一阶段主线是：

**P3 做实 → 建 benchmark → 跑最小训练矩阵 → 保留对复盘或搜索真正有效的 head → 再考虑 P5。**
