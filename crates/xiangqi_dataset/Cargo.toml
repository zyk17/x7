[package]
name = "xiangqi_dataset"
version = "0.1.0"
edition.workspace = true
description = "Maintainer dataset helpers: PGN parsing and future self-play preprocessing."
publish = false

[dependencies]
anyhow = "1"
regex = "1"

[dev-dependencies]
